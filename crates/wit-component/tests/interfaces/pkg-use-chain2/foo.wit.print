package foo:foo

interface other {
  record name {
    f: u8,
  }
}

interface bar {
  record the-name {
    f: u8,
  }

  enum name {
    a,
  }
}

interface foo {
  record the-name {
    f: u8,
  }
}

