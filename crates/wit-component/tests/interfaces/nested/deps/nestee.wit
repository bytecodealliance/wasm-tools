package foo:nestee;

interface things {
  //nesting can be documented
  @since(version = 1.0.0)
  nest foo:nestnest/deep;
  record nestrecord {
    foo: string
  }
  hello: func() -> string;
}

interface more {
  resource foo {
    bar: func() -> option<string>;
  }
}

interface usable {
  record usable-record {
    foo: string
  }
}