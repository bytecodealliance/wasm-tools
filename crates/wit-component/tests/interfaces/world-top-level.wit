world foo {
  import foo: func()
  export foo: func()

  import bar: func(arg: u32)
  export bar: func() -> u32

  import some-interface: interface {}
  export another-interface: interface {}
}

world just-import {
  import foo: func()
}

world just-export {
  export foo: func()
}
