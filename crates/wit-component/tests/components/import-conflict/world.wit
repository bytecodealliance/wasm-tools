interface foo {
  a: func()
}

interface bar {
  a: func(x: u64, y: string)
}

interface baz {
  baz: func(x: list<u8>) -> list<u8>
}

world my-world {
  import bar: bar
  import baz: baz
  import foo: foo
}
