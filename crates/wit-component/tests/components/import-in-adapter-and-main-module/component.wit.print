package component

world root {
  import shared-dependency/doc
  import shared-dependency/types
  import main-dep: interface {
    use shared-dependency/types.{a-typedef}
    foo: func() -> a-typedef
  }
  import adapter-dep: interface {
    use shared-dependency/types.{a-typedef}
    foo: func() -> a-typedef
  }
}
