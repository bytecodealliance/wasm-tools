basic-record(// { required: 1 });
basic-record({ required: 1 } // );
basic-record(// {
  required: 1,
});
basic-record(
  {
    required: 1,
//}
);
basic-record({
  // required: 1,
  optional: none,
});