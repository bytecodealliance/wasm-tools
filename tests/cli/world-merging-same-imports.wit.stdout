package root:root;

world root {
  import a: interface {
  }
  import a:b/b;
  use a:b/b.{t};
  import c: func();
}
/// RUN: component embed --dummy --world into % | component embed --world %from % | component wit
package a:b {
  interface b {
    type t = u32;
  }
  world into {
    import a: interface {
    }
    import b;
    use b.{t};
    import c: func();
  }
  world %from {
    import a: interface {
    }
    import b;
    use b.{t};
    import c: func();
  }
}
