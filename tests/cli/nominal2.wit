// RUN: component wit --generate-nominal-type-ids foo % --json

package a:b;

interface x {
  resource r;
  hi: func() -> r;
}

world foo {
  import x;
  export x;

  export y: interface {
    use x.{r};
  }
}
