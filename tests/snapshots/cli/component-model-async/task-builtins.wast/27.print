(component
  (core module $m (;0;)
    (type (;0;) (func (param i32) (result i32)))
    (import "" "subtask.cancel" (func $subtask-cancel (;0;) (type 0)))
  )
  (core func $subtask-cancel (;0;) (canon subtask.cancel))
  (core instance (;0;)
    (export "subtask.cancel" (func $subtask-cancel))
  )
  (core instance $i (;1;) (instantiate $m
      (with "" (instance 0))
    )
  )
)
