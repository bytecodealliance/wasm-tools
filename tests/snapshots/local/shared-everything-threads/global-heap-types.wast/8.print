(module
  (import "spectest" "global_exnref" (global (;0;) (shared (ref null (shared exn)))))
  (import "spectest" "global_mut_exnref" (global (;1;) (shared mut (ref null (shared exn)))))
  (import "spectest" "global_ref_null_exn" (global (;2;) (shared (ref null (shared exn)))))
  (import "spectest" "global_mut_ref_null_exn" (global (;3;) (shared mut (ref null (shared exn)))))
  (import "spectest" "global_ref_exn" (global (;4;) (shared (ref (shared exn)))))
  (import "spectest" "global_mut_ref_exn" (global (;5;) (shared mut (ref (shared exn)))))
  (global (;6;) (shared (ref null (shared exn))) ref.null (shared exn))
  (global (;7;) (shared (ref null (shared exn))) ref.null (shared exn))
)
