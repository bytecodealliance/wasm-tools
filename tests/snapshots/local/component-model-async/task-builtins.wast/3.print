(component
  (core module $libc (;0;)
    (memory (;0;) 1)
    (export "memory" (memory 0))
  )
  (core instance $libc (;0;) (instantiate $libc))
  (core module $m (;1;)
    (type (;0;) (func (param i32) (result i32)))
    (import "" "task.wait" (func $task-wait (;0;) (type 0)))
  )
  (alias core export $libc "memory" (core memory (;0;)))
  (core func $task-wait (;0;) (canon task.wait async (memory 0)))
  (core instance (;1;)
    (export "task.wait" (func $task-wait))
  )
  (core instance $i (;2;) (instantiate $m
      (with "" (instance 1))
    )
  )
)
