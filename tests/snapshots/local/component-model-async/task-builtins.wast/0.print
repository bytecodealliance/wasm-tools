(component
  (core module $m (;0;)
    (type (;0;) (func (param i32)))
    (import "" "task.backpressure" (func $task-backpressure (;0;) (type 0)))
  )
  (core func $task-backpressure (;0;) (canon task.backpressure))
  (core instance (;0;)
    (export "task.backpressure" (func $task-backpressure))
  )
  (core instance $i (;1;) (instantiate $m
      (with "" (instance 0))
    )
  )
)
