(module
  (import "spectest" "memory" (memory (;0;) 1))
  (import "spectest" "memory" (memory (;1;) 0))
  (import "spectest" "memory" (memory (;2;) 1 2))
  (import "spectest" "memory" (memory (;3;) 0 2))
  (import "spectest" "memory" (memory (;4;) 1 3))
  (import "spectest" "memory" (memory (;5;) 0 3))
)