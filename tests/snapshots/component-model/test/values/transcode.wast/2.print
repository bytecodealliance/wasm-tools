(component
  (component $C (;0;)
    (core module $M (;0;)
      (type (;0;) (func (param i32 i32 i32 i32) (result i32)))
      (type (;1;) (func (param i32 i32) (result i32)))
      (memory (;0;) 1)
      (global $next (;0;) (mut i32) i32.const 1024)
      (export "mem" (memory 0))
      (export "realloc" (func 0))
      (export "join" (func 1))
      (func (;0;) (type 0) (param $old i32) (param $osize i32) (param $align i32) (param $nsize i32) (result i32)
        (local $r i32)
        local.get $old
        i32.const 0
        i32.ne
        local.get $nsize
        local.get $osize
        i32.le_u
        i32.and
        if ;; label = @1
          local.get $old
          return
        end
        global.get $next
        i32.const 7
        i32.add
        i32.const -8
        i32.and
        global.set $next
        global.get $next
        local.set $r
        global.get $next
        local.get $nsize
        i32.add
        global.set $next
        local.get $old
        i32.const 0
        i32.ne
        if ;; label = @1
          local.get $r
          local.get $old
          local.get $osize
          memory.copy
        end
        local.get $r
      )
      (func (;1;) (type 1) (param $ptr i32) (param $units i32) (result i32)
        local.get $units
        i32.const 5
        i32.ne
        if ;; label = @1
          unreachable
        end
        local.get $ptr
        i64.load
        i64.const -2865373468571598744
        i64.ne
        if ;; label = @1
          unreachable
        end
        local.get $ptr
        i32.load16_u offset=8
        i32.const 57200
        i32.ne
        if ;; label = @1
          unreachable
        end
        i32.const 0
        i32.const 64
        i32.store
        i32.const 4
        i32.const 4
        i32.store
        i32.const 0
      )
      (data (;0;) (i32.const 64) "\f6\00k\00<\d8p\df")
    )
    (core instance $m (;0;) (instantiate $M))
    (type (;0;) (func (param "s" string) (result string)))
    (alias core export $m "join" (core func (;0;)))
    (alias core export $m "mem" (core memory (;0;)))
    (alias core export $m "realloc" (core func (;1;)))
    (func (;0;) (type 0) (canon lift (core func 0) string-encoding=utf16 (memory 0) (realloc 1)))
    (export (;1;) "join" (func 0))
  )
  (component $D (;1;)
    (type (;0;) (func (param "s" string) (result string)))
    (import "join" (func $join (;0;) (type 0)))
    (core module $Libc (;0;)
      (type (;0;) (func (param i32 i32 i32 i32) (result i32)))
      (memory (;0;) 1)
      (global $next (;0;) (mut i32) i32.const 1024)
      (export "mem" (memory 0))
      (export "realloc" (func 0))
      (func (;0;) (type 0) (param $old i32) (param $osize i32) (param $align i32) (param $nsize i32) (result i32)
        (local $r i32)
        local.get $old
        i32.const 0
        i32.ne
        local.get $nsize
        local.get $osize
        i32.le_u
        i32.and
        if ;; label = @1
          local.get $old
          return
        end
        global.get $next
        i32.const 7
        i32.add
        i32.const -8
        i32.and
        global.set $next
        global.get $next
        local.set $r
        global.get $next
        local.get $nsize
        i32.add
        global.set $next
        local.get $old
        i32.const 0
        i32.ne
        if ;; label = @1
          local.get $r
          local.get $old
          local.get $osize
          memory.copy
        end
        local.get $r
      )
    )
    (core instance $libc (;0;) (instantiate $Libc))
    (alias core export $libc "mem" (core memory (;0;)))
    (alias core export $libc "realloc" (core func (;0;)))
    (core func $join' (;1;) (canon lower (func $join) (memory 0) (realloc 0)))
    (core module $Main (;1;)
      (type (;0;) (func (param i32 i32 i32)))
      (type (;1;) (func (result i32)))
      (import "" "mem" (memory (;0;) 1))
      (import "" "join" (func $join (;0;) (type 0)))
      (export "run" (func 1))
      (func (;1;) (type 1) (result i32)
        (local $p i32)
        i32.const 16
        i32.const 10
        i32.const 8
        call $join
        i32.const 8
        i32.load
        local.set $p
        i32.const 12
        i32.load
        i32.const 7
        i32.ne
        if ;; label = @1
          unreachable
        end
        local.get $p
        i32.load
        i32.const -261376317
        i32.ne
        if ;; label = @1
          unreachable
        end
        local.get $p
        i32.load16_u offset=4
        i32.const 36255
        i32.ne
        if ;; label = @1
          unreachable
        end
        local.get $p
        i32.load8_u offset=6
        i32.const 176
        i32.ne
        if ;; label = @1
          unreachable
        end
        i32.const 43
      )
      (data (;0;) (i32.const 16) "h\c3\b6\e2\98\83\f0\9f\8d\b0")
    )
    (alias core export $libc "mem" (core memory (;1;)))
    (core instance (;1;)
      (export "mem" (memory 1))
      (export "join" (func $join'))
    )
    (core instance $main (;2;) (instantiate $Main
        (with "" (instance 1))
      )
    )
    (type (;1;) (func (result u32)))
    (alias core export $main "run" (core func (;2;)))
    (func (;1;) (type 1) (canon lift (core func 2)))
    (export (;2;) "run" (func 1))
  )
  (instance $c (;0;) (instantiate $C))
  (alias export $c "join" (func (;0;)))
  (instance $d (;1;) (instantiate $D
      (with "join" (func 0))
    )
  )
  (alias export $d "run" (func (;1;)))
  (export (;2;) "run" (func 1))
)
